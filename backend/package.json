{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend server to securely fetch stock data for a trading dashboard. This project acts as a proxy to hide the API key from the frontend.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js"
  },
  "author": "Sarthak Bansal",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.11.0",
    "cors": "^2.8.5",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "dotenv": "^17.2.2"
  }
}
